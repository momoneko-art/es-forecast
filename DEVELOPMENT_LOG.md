# 開発メモ (Development Log)

このファイルは、Es予測ダッシュボードの設計判断・修正履歴・未対応事項を記録する開発者向けメモです。
（次にこのプロジェクトを触るAIセッション、および将来の自分自身が経緯を追えるように残しています）

## アーキテクチャ概要

- `scripts/fetch_pskreporter.py` — PSKReporterから直近15分のFT8受信報告(10m/6m)を取得
- `scripts/fetch_nict.py` — NICT電離圏概況ページから各地点の実測foEs(E層臨界周波数)をスクレイピング。最優先のground-truth証拠として扱う
- `scripts/fetch_noaa.py` — NOAA SWPCから (1) 現在のKp指数の実測値、(2) 今後24時間以内のKp"予報"のピーク値、の両方を取得
- `scripts/fetch_tropo.py` — Open-MeteoのGFSデータから対流圏ダクト(トロポダクト)指数を計算。地表付近の気圧面(1000/975/950/925/900/850/700hPa)で気温減率を見て、負の勾配(逆転層)を検出
- `scripts/climatology.py` — 季節・時刻からの統計的Es活動ベースライン + `nict_floor_from_foes()` (実測floor)
- `scripts/heatmap.py` — 全国グリッドへの空間補間(気候ベースライン+PSKReporter+NICT floor)
- `scripts/fetch_jetstream.py` — 【実験・未反映】250hPaジェット気流風速の記録専用(下記「ジェット気流の記録」参照)
- `scripts/build_index.py` — 上記を統合しGitHub Actions上で15分ごとに実行、`data.json`を生成
- `es-forecast-agent/agent.py` — build_index.pyと同等のロジックを持つ、ユーザーのPCで常時稼働するスタンドアロン版(GitHub Contents API経由でdata.json等を直接push)。**gitでは管理していない**ファイル
- `index.html` — `data.json`を読み込んで表示する静的ダッシュボード(GitHub Pages)

### es_indexの算出式(要点)
```
combined_boost = 1 - (1-evidence_boost) * (1-nict_boost)   # PSKReporterとNICTのnoisy-OR
es_index = clima * (1 + 0.6 * combined_boost)
es_index = max(es_index, nict_floor_from_foes(foes_mhz))   # NICT実測は気候ベースラインでキャップされない
es_index = clamp(0, 100)
```
`nict_floor_from_foes()`はMUF(oblique)≈3×foEsの経験則に基づき、foEsをそのまま0-100スケールの下限値に変換する(climatology.py参照)。

### Kp予報の活用
`kp_for_modifier = max(実測Kp, 今後24時間以内のKp予報ピーク)` を`climatology_index()`に渡すことで、稚内(aurora_sensitive)などオーロラ性Esが起きやすい局は、実際にKpが上がる前から予報ベースで先行して指数が上がる。`aurora_forecast_active`フラグでUIにバッジ表示。

### NICT単発欠測の引き継ぎ(2026-09-02追加)
NICTのスクレイプが1サイクルだけ`unknown`になる(電離層観測の瞬間的な穴、または一時的なページ取得失敗)ケースに対応。直前の「実測OK」だった値を`NICT_CARRY_FORWARD_MAX_SECONDS`(45分)以内なら引き継ぎ、`stale_minutes`をUIに表示する。**元の測定時刻を保持したまま**経過時間をカウントするため、欠測が連続しても正しく期限切れになる。

### データ取得状況パネル(2026-09-02追加)
`build_index.py`/`agent.py`の`summarize_status()`が、各上流データソース(PSKReporter・NICT・NOAA Kp実測・NOAA Kp予報・GFSダクト)ごとに`ok`/`warn`/`error`の平易な状態を`data.json`の`system_status`に出力し、`index.html`のヘッドライン直下にピル表示する。**ユーザーが対処するための機能ではなく**(外部サービスの障害はユーザー側でどうにもできないため)、数値が控えめに見える理由を一目で分かるようにする、また不具合報告時のスクリーンショットから即座に原因箇所を特定できるようにするための表示。

### ジェット気流の記録(実験・2026-09-02追加、未反映)
`scripts/fetch_jetstream.py`(agent.pyでは`fetch_jetstream()`関数として同等ロジックをミラー)が、Open-MeteoのGFSから250hPa(ジェット気流高度)の風速を4地点平均で取得し、`history.csv`の`jet250_kmh`列と`data.json`の`jet250`に記録する。**es_indexやヒートマップの計算には一切使っていない**、将来のバックテスト用のデータ収集のみの機能。2026-09-02時点でhistory.csvはまだ1日分程度しか無く、相関を検証できる段階ではないため、まずはデータを溜める段階。

### 地図の配色・グリッド補間(2026-09-02追加)
- **トロポダクト指数の配色をEsヒートマップと統一**: 元々`TROPO_STOPS_LIGHT`/`DARK`は青→紫→マゼンタでEsマップ(緑→黄→橙→赤)と意図的に別配色にしていたが、「同じ色使いの方が分かりやすい」とのフィードバックで`TROPO_STOPS_* = HEAT_STOPS_*`(参照共有)に変更。両マップの区別はパネル見出しのみで付ける
- **ダクトマップの視覚補間(bilinear)**: 実際に取得している気象格子は2度間隔(Open-Meteoへの通信回数の都合上、Esマップの0.5度より粗い)。`bilinearTropoValue()`を追加し、`renderTropoCanvas()`とホバーツールチップの両方で周辺4点から補間した値を使って描画。`SUBDIV`定数(細分化倍率)で見た目の解像度を制御。**取得データそのものの精度は変わらない**(表示上の滑らかさのみ)。初め4(0.5度相当)→ユーザー希望でさらに8(0.25度相当、Esマップより細かい)に変更
- **ダークモードの0値色を背景色に統一**: `HEAT_STOPS_DARK`(Es/トロポ両マップ共通)の0.00ストップが暗緑`[40,68,48]`のままだったため、値が低い/無い区域がマップパネルの紺背景(`--panel:#161f2a`)から独立した色面として目立って見えていた。0.00ストップをパネル背景色そのもの`[22,31,42]`に変更し、活動が弱い区域は自然に背景へ溶け込むようにした(ライトモードは元々目立たなかったため未変更)

## 修正履歴 (このセッションでの主な変更)

| 日付 | 内容 |
|---|---|
| 2026-09-01 | **es_index計算式の欠陥修正**: 気候ベースラインが低い時間帯だと、NICTの強い実測値があってもes_indexが上がらなかった問題。`nict_floor_from_foes()`を追加し、実測値が気候ベースラインを下限突破できるように修正 |
| 2026-09-01 | **トロポダクト指数の過小評価バグ修正**: 気圧面のサンプリングが粗すぎ(1000/925/850/700hPaで650-1500mの間隔)、実際の薄い(200-500m)ダクト層が平均化されて消えていた。Open-Meteoが提供する近地表の気圧面を全て使うよう拡張(1000/975/950/925/900/850/700hPa)。`INDEX_VERSION`定数で旧式のキャッシュを強制的に再取得させるゲートも追加 |
| 2026-09-01 | 自局Es交信チャンスパネルの表示順を、自局登録の直後・地図の前に変更 |
| 2026-09-01 | 各局のトレンド矢印を直近4サイクル分の履歴として表示(↓→↑↑のような連続矢印)。2回連続上昇は赤色でハイライト |
| 2026-09-02 | **NOAA Kp"予報"(3日先)の組み込み**: 実測Kpだけでなく、今後24時間以内の予報ピークも見てオーロラ性Es局の指数を先行して上げるように |
| 2026-09-02 | **GitHub Actions push失敗の修正**: PC常駐エージェントとGitHub Actionsが同じmainブランチへ同時にpushしてrace conditionが起きていた問題。fetch+rebase+リトライ(Actions側)、sha再取得+リトライ(agent.py側)を追加。**PATに`Workflows: Read and write`権限が必要**で、これがないとワークフローファイル自体の更新がGitHubにブロックされる(2026-09-02にユーザーが権限追加、解決済み) |
| 2026-09-02 | **NICT単発欠測の引き継ぎ機能**: 上記「NICT単発欠測の引き継ぎ」参照。稚内で実際に1サイクルだけ欠測が起き、直前の11-12MHzの実測値が反映されなかった実例をきっかけに実装 |
| 2026-09-02 | **データ取得状況パネル追加**: 各上流データソースのok/warn/error状態を平易な言葉でダッシュボードに表示 |
| 2026-09-02 | **ジェット気流データの記録開始(実験・未反映)**: HRO/RMOB流星電波観測データの自動取得可否も調査したが、画像グラフのみ・個人運営サイトの寄せ集めで自動取得が非現実的と判断し保留。ジェット気流の方はOpen-Meteoで250hPa風速がそのまま取得できることを確認し、まずは`jet250_kmh`としてhistory.csvへの記録を開始(バックテストは数週間〜のデータ蓄積後) |
| 2026-09-02 | **dxinfocentre.comとの見た目の差異を調査**: ユーザーがdxinfocentreのトロポマップと自アプリの差異を指摘。実際はユーザーが見ていたのが4日前のアーカイブ画像(Sat Aug 29付)で、最新の同サイトの図・自アプリのグリッド値ともに「中程度」で一致しており、不具合ではなかった(Claude-in-Chromeブラウザ接続で直接サイトを操作して確認) |
| 2026-09-02 | **地図の配色統一・グリッド補間・ダークモード0値色の調整**: 上記「地図の配色・グリッド補間」参照 |
| 2026-09-03 | **トロポダクトパネルの追加機能**: DX Info Centreへのリンク追加、時刻表示にGMT併記、+5日クイックジャンプボタン追加。詳細は上記「トロポダクトパネルの追加機能」参照 |
| 2026-09-03 | **ECMWF併用・kc2g予備経路・F10.7記録の追加**: 前回調査した3候補(ECMWF/kc2g/F10.7)を全て実装。詳細は下記「ECMWF併用・kc2g予備経路・F10.7記録」参照 |
| 2026-09-03 | **リンクパネルの階層化・ヘッドラインへの地名表示**: リンク一覧を「実際に使用しているデータ元/予備データ元/記録のみ・実験中/参考リンク」の4段に整理。ヘッドラインカードの数値の横に該当エリア名を表示(例: `52 山川`) |
| 2026-09-03 | **トロポダクトパネルの自動再生・タップ地点マーカー**: +7日ボタンの隣に再生ボタンを追加し、時間スライダーを自動でループ再生できるように。フィードバックを受けて2回改良(地図タップは再生「停止」専用に変更/固定位置の読み取りバー化/タップ地点への目印マーカー追加)。詳細は各コミットメッセージ参照 |
| 2026-09-03 | **Es・トロポ凡例に「数値の目安」を常時表示**: 色の変わり目(30/50/80、15/35/60)が実際どれくらいの交信可能性を意味するのかを、凡例の下に常時表示するテキストとして追記 |
| 2026-09-04 | **予報ロジックの高度化(時間ラグ特徴量・Skill Score自己診断)**: 他局のブログ記事(EDFS)を参考に、Ridge回帰へ時間ラグ特徴量とホールドアウト自己診断を追加。詳細は下記「予報ロジックの高度化」参照 |

### トロポダクトパネルの追加機能(2026-09-03追加)
- 一次情報へのリンクに **DX Info Centre のトロポダクト予報(Far East)** (`tropo_eas.html`)を追加。ダクト比較調査で参照したサイトで、自アプリの値との見比べ用
- 時刻表示を **JSTに加えてGMT(UTC)も併記**するように`formatTropoTime()`を変更(例: `9/3(木) 21:00 JST ／ 9/3(木) 12:00 GMT`)。日付をまたぐ場合もそれぞれ正しい日付で表示される
- クイックジャンプボタンに **+5日** を追加(現在/+1日/+3日/+5日/+7日)。`jumpTropoTo()`はもともと任意の日数オフセットに対応した汎用ロジックだったため、ボタンを1つ増やすだけで動いた

### 追加データソース調査(2026-09-03)
ユーザー依頼で「Es予報・ダクト予報に使えそうな他の公開データ」をWeb調査。まだ何も実装していない、候補の記録のみ。

**Es予報向け候補:**
- **ECMWF (IFS HRES) via Open-Meteo** ★最有力: 2025-10-01にECMWFがOpen-Meteo経由で無料公開(`/v1/ecmwf`、APIキー不要、CC-BY 4.0)。今使ってるGFS(`fetch_tropo.py`)と全く同じOpen-Meteoのリクエスト形式・変数体系(気温/湿度/気圧面)で、モデル名を変えるだけで使える。解像度9km(GFSより高精度)、6時間毎更新、1時間刻みは90時間先まで。**ダクト計算の精度向上に使える可能性が高く、実装コストが低い**
- **kc2g.com/GIRO電離層観測アグリゲータ**: `https://prop.kc2g.com/api/stations.json` (JSON、認証不要)。GIRO+NOAA+SWS Australiaの世界中のデジゾンデを集約。**確認したところ、うちが使っている稚内(WK546)・国分寺(TO536)・山川(YG431)・大宜味に近い沖縄(OK426)がまさに同じ地点として載っており、`source:"giro"`となっていることから、NICTのデータをGIRO経由で再配信しているものと推測される(＝新しい独立データではなく、既存のNICT実測を構造化JSONで取れる代替経路である可能性が高い)**。ただしfoEsに加えfoF2・MUF(3000km)・韓国(済州島・利川)やロシア(ハバロフスク)の隣接局データも同梱されており、①NICTページのスクレイピングが壊れた時のフォールバック、②隣接局の動向から国内への飛来を先読みする早期警戒、の2つの用途で価値がありそう。ただしこのドメインもクラウドのサンドボックス自体からは未検証(dxinfocentreと同様、GitHub Actions側からの到達性は別途確認が必要)
- **NOAA F10.7太陽電波束**: `https://services.swpc.noaa.gov/json/f107_cm_flux.json`(実測)+`predicted_f107cm_flux.json`(予報)。今使ってるKp指数と全く同じドメイン・パターンなので追加は容易。ただし太陽活動の長期的な電離層電離度の指標であり、Es(E層)への直接的な予報価値は限定的(F2層への影響の方が大きい)と考えられ、優先度は低め
- **GNSS TEC(全電子数)マップ**(NASA CDDIS、GFZ等): 電離層全体の電子数を面的に見られるが、Es層だけを分離した指標ではなく、Esとの相関は間接的。今回は見送り
- **風のシア(wind shear)理論に基づく直接予測**: Esの物理的な主因(E層高度の風の急変)そのものを使う研究は進んでいる(COSMIC-2電波掩蔽観測など)が、無料の実用API化はされておらず、研究レベル。現状では実装不可能と判断

**トロポダクト予報向け候補:**
- **ECMWF via Open-Meteo**: 上記と同じ(Es・トロポ両方に効く共通の改善候補)
- **高層観測実測(ラジオゾンデ)**: University of Wyoming Upper Air archive(`weather.uwyo.edu/upperair/`、無料・登録不要・テキスト形式)経由で、輪島・館野・鹿児島など日本の実測気球観測データが取れる。GFSモデルの「予報」ではなく「実測」なので、NICTのfoEsと同じ位置づけの検証・キャリブレーション用データになりうる。ただし1日2回(00Z/12Z)・地点固定(面ではなく点)なので、現在の面的グリッド計算を置き換えるものではなく、あくまで精度検証用の補助データ

上記3候補(ECMWF・kc2g・F10.7)は2026-09-03に実装済み(下記「ECMWF併用・kc2g予備経路・F10.7記録」参照)。GNSS TECと風のシア理論は引き続き見送り。ラジオゾンデ実測(University of Wyoming)は未実装のまま(下記「未対応・保留中の項目」参照)。

### ECMWF併用・kc2g予備経路・F10.7記録(2026-09-03追加)
ユーザーから「全て併用して精度を上げたい」との依頼を受け、上記調査の3候補を全て実装。**慎重にエラーが出ないよう**という指示のもと、いずれも「新データが無くても/取得失敗しても元の動作に自動フォールバックする」設計にしてある。

- **トロポダクト指数へのECMWF併用**(`fetch_tropo.py`/`agent.py`の`fetch_ecmwf_grid()`): Open-MeteoでGFSに加えて`models=ecmwf_ifs`を取得し、ダクト**指数レベル**でブレンド(GFS 65% + ECMWF 35%、重みは`ECMWF_BLEND_WEIGHT`定数)。生の気圧面値同士を平均しているわけではない — ECMWFのOpen-Meteo提供分は975/950/900hPaが無い(1000/925/850/700hPaのみ)ため、生値で平均すると2026-09-01に修正したばかりの「薄いダクト層が消える」バグをECMWF側で再発させてしまう。そのため各モデルで**それぞれ**指数を計算してから合成する方式にした。また、ECMWFはOpen-Meteo上で最初の約90時間のみ1時間刻み、それ以降は3時間刻み→6時間刻みと粗くなり、GFSと時間軸の刻みが一致しないため、配列のインデックスではなく**ISO8601タイムスタンプ文字列で突き合わせて**ブレンドする(`_entry_to_ecmwf_index_by_time()`)。ECMWF取得が失敗した場合は例外を握りつぶしてGFS単独の値をそのまま使う(`ecmwf.used=false`, `ecmwf.error`に理由)。`INDEX_VERSION`を3に上げて旧キャッシュを無効化。**api.open-meteo.comはクラウドサンドボックスからもPC側からも到達確認ができず(robots.txt/ネットワーク制限)、ブレンドロジック自体はモック実測値での単体テストで検証済みだが、実際のAPIレスポンス形状はGitHub Actions/agent.pyの実運用サイクルで初めて確認できる**
- **kc2g/GIRO電離層観測の予備経路**(新規`fetch_kc2g.py`/`agent.py`の`fetch_kc2g()`): kc2gはNICTと同じ実測(GIRO経由の再配信と推測される、`source:"giro"`)なので、**NICT自身のスクレイピング成功時は一切使わない**。NICTのスクレイプ失敗**かつ**45分間の引き継ぎ(carry-forward)も期限切れの場合のみ、その局のfoEsをkc2gの値で代替する(最終手段のフォールバック)。二重計上を避けるため、NICTとkc2gを平均する、といったことは行っていない。どの経路で値が来たかは各局の`nict.source`(`nict`/`nict_carry_forward`/`kc2g_fallback`)としてdata.jsonに記録
- **F10.7太陽電波束の記録専用ログ**(新規`fetch_f107.py`): NOAA SWPCから取得し、`history.csv`の`f107`列に記録するのみで、es_index計算にはまだ使っていない(ジェット気流の`jet250_kmh`と同じ「まずデータを溜める」方針)。kc2gから取得できる済州島・利川・ハバロフスクなど東アジア隣接局のfoEsも同様に`kc2g_<局コード>_foes`列として記録専用でログ開始
- **データ取得状況パネルの拡張**: kc2g予備経路とF10.7の状態も`summarize_status()`に追加(失敗時は`warn`扱い、NICT本体やダクト予報自体を`error`にはしない)。ダクト予報の項目名を「ダクト予報(GFS)」→「ダクト予報(GFS+ECMWF)」に変更し、ECMWFがブレンドできた/できなかったを注記で表示
- **リンクパネルへの追加**: Open-Meteoを「実際に使用しているデータ元」に、kc2gを新設した「予備データ元(NICT取得失敗時のみ使用)」に、F10.7を新設した「記録のみ・まだ予報には未反映(実験中)」にそれぞれ追加

### 予報ロジックの高度化(2026-09-04追加)
ユーザーが読んでいた他局のブログ記事「Esスポラディック層解析プログラムEDFS」(https://ameblo.jp/jl7khn/entry-12976221040.html)を調査したところ、うちと**同じ4局(稚内・国分寺・山川・沖縄)のNICT実測とPSKReporterを核データに使っている**ことが分かった一方、EDFSはF層マスキング・MSTID画像解析・宇宙天気フルセット・時間ラグ特徴量+自己診断付きの高度なRidge回帰パイプラインなど、うちには無い要素も複数持っていた。ユーザーに「どれを取り入れたいか」複数選択で確認した結果、**「予報ロジックの高度化(時間ラグ特徴量・自己診断)」のみ**が選ばれ、F層マスキング・宇宙天気拡充・MSTID画像解析は今回スコープ外とした(F層マスキングは現在のNICTスクレイプ元(簡易ステータスページ)にfoF2/hmF2が無く別途調査が必要、MSTID画像解析は技術的難易度が高く以前から保留中)。

`build_index.py`/`agent.py`の`try_train_models()`/`predict_expected_count()`を全面的に書き換え:
- **時間ラグ特徴量**: 各局の6mスポット数について、直近15/30/45/60分前の実測値を特徴量に追加(`LAG_MINUTES`)。`_lagged_targets_for_row()`が、対象行より**古い行だけ**(リーク防止)を許容誤差±7分(`LAG_TOLERANCE_MINUTES`、履歴のサイクル間隔のブレを吸収)以内で探す。該当が無い場合は0ではなく「直近で変化なし」を意味する現在値そのものにフォールバックする(0にすると、データが疎な期間に不自然な急落があったことにされてモデルを歪めてしまうため)
- **Skill Score自己診断**: 直近20%(最低15行、`HOLDOUT_FRACTION`/`MIN_HOLDOUT_ROWS`)をホールドアウトとして取り分け、Ridgeモデルの予測誤差(MSE)を「15分前の実測値をそのまま使うだけ」のPersistenceベースラインと比較。`Skill Score = 1 - MSE_model/MSE_persistence`を算出し、**0以下(素朴なpersistenceにも勝てていない)局はモデルの予測を使わず`climatology_only`にフォールバックする**。ホールドアウト行数が足りない場合はSkill Score自体を算出せず(`None`)、判定材料が無いので保守的にモデル不使用とする。局ごとのモデル使用状況は`model`フィールド(例: `ridge(n=300, skill+0.27)` / `climatology_only(ridge skill-0.20<=0)`)としてUIの各局カードに表示済み(コード変更不要、既存の`model-tag`表示がそのまま反映)
- 学習・推論の特徴量のズレを防ぐため、`try_train_models()`が「現在時刻」用の特徴ベクトル(ラグ込み)を`last_features`として計算・保存し、`predict_expected_count()`はそれをそのまま使う(dt/kpを受け取って再計算し直す旧方式から変更)
- モック履歴データ(momentum/regime-shift/sparse-dataなど8パターン)でSkill Scoreが正負どちらにも正しく振れることを確認してから本番投入。`try_train_models()`呼び出し全体をtry/exceptで囲み、万一新ロジックにバグがあってもクラッシュせず全局`climatology_only`にフォールバックするようにした
- EDFS記事にあったEs状態遷移フェーズ・Permutation Importance/VIF・ドリフト検出による条件付き再学習は今回のスコープ外(既存通り毎サイクル素朴に再学習し直す方式のまま) - 複雑さとバグの入る余地を増やさないための判断

## 未対応・保留中の項目

- **F層マスキング**: EDFS記事を参考にした候補。27MHz帯の交信がEs由来かF層(F2層)由来かを`foF2/fxEs`比で判定する仕組み。実装には現在のNICTスクレイプ元(簡易ステータスページ)には無いfoF2/hmF2データの取得元調査が別途必要
- **宇宙天気データの拡充(太陽風・Bz)**: EDFS記事を参考にした候補。NOAA SWPCに太陽風速度・磁場Bzの無料APIがあり、F10.7と同様にまずは記録専用(shadow logging)から始める想定。今回は見送り
- **MSTID画像解析**: EDFS記事を参考にした候補。個人サイトの画像を機械的に解析するのは技術的難易度・安定性ともに高く、以前からの保留項目
- **ATOM Cam2連携**: 27.005MHz違法無線のATOM Cam2音声通知をフィードバックループに組み込む案(①手動ボタン／②公式SoraCam Webhook／③atomcam_tools自作改造)を3案提示済み。ユーザー未選択
- **短期トレンド外挿(精度向上ロードマップ①)**: 既存のtrend_historyと気候曲線の形状を使った短期予測。未実装
- **ジェット気流相関の検証(精度向上ロードマップ③)**: `jet250_kmh`の記録は開始済み(2026-09-02〜)。history.csvに数週間〜1シーズン分のデータが溜まったら、実際にEs発生と相関があるかバックテストして、根拠が確認できたらes_indexへの反映を検討する
- **HRO/RMOB流星電波観測データ**: 調査の結果、自動取得は非現実的と判断し保留(詳細は上記アーキテクチャ概要参照)。個人運営サイトの状況が変わった場合は再検討の余地あり
- 通知機能、foF2データ追加(いずれも古いメモにあった未着手項目)

## デプロイ手順

`es-forecast-deploy`スキル参照。要点:
- クラウドのサンドボックスから直接GitHubへpushできないため、必ずユーザーのPC(OneDriveフォルダ`es-forecast`)経由でgit操作する
- GitHub Actionsが15分ごとに自動push(`[skip ci]`)しているため、push前に必ず`fetch`+`rebase`する
- PAT(Personal Access Token)はどのファイルにも保存せず、`$HOME/mnt/es-forecast/Personal Access Token.txt`から都度読み込んで使う
- `agent.py`はgit管理外。`SendUserFile`→`device_commit_files`でOneDriveの`es-forecast-agent/`フォルダへ直接転送し、ユーザーに再起動してもらう
